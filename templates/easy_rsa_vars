export KEY_COUNTRY="{{ openvpn_country }}"
export KEY_PROVINCE="{{ openvpn_province }}"
export KEY_CITY="{{ openvpn_city }}"
export KEY_ORG="{{ openvpn_org }}"
export KEY_EMAIL="{{ openvpn_email }}"
export KEY_CN="{{ openvpn_cn }}"
export KEY_NAME="{{ openvpn_name }}"
export KEY_OU="{{ openvpn_ou }}"
